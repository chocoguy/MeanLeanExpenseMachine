#include <wx/wx.h>
#include "MLEMGui.h"

class MainScreen : public wxFrame
{
	MainScreen(const wxString& title, const wxPoint& pos, const wxSize& size) : wxFrame(NULL, wxID_ANY, title, pos, size)
	{

	}

	void RenderHomePanel()
	{


	}

};
